## talosctlenv list-remote

List all remote talosctl versions from GitHub (sorted by semver)

### Synopsis

List all talosctl versions available on GitHub, sorted by semver.

In the list the versions currently installed are marked with a '+' symbol, while the version currently in use is marked with a '*' symbol.

Note: By default pre-release versions (alpha, beta, rc) are hidden. Use '--devel' to include them

```
talosctlenv list-remote [flags]
```

### Options

```
      --devel       Include pre-release versions (alpha, beta, rc)
  -h, --help        help for list-remote
  -l, --limit int   Limit number of versions displayed
```

### Options inherited from parent commands

```
  -b, --bin-path string   Absolute path to folder storing in-use talosctl binary (default "/home/user/.talosctlenv/bin")
  -d, --vrs-path string   Absolute path to folder storing downloaded talosctl binary versions (default "/home/user/.talosctlenv/versions")
```

### SEE ALSO

* [talosctlenv](talosctlenv.md)	 - A talosctl versions manager

###### Auto generated by spf13/cobra on 13-Nov-2025
